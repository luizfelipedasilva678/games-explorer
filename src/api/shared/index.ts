import { createErrorObject } from "./create-error-object";

const HTTP_NOT_FOUND = 404;
const HTTP_INTERNAL_SERVER_ERROR = 500;

export { createErrorObject, HTTP_NOT_FOUND, HTTP_INTERNAL_SERVER_ERROR };
